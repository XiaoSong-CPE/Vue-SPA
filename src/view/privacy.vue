<template>
    <n-h1>Privacy</n-h1>
    <n-p>By registering in this website you know and understand our privacy policy below.</n-p>
    <n-p>This website has no ability to protect your privacy, so you should not post any sensitive infomation.</n-p>
    <n-p>Your password will not be encrypted as well, so never use a password that is the same as other passwords of
        yours.</n-p>
</template>
<!-- 
<script setup lang="ts">
import { RouterLink, RouterView, useRouter, useRoute } from 'vue-router'
import { onMounted } from 'vue'
import { useLoadingBar } from 'naive-ui'
const loadingBar = useLoadingBar()
onMounted(() => {
    loadingBar.finish()
})
console.log(useRoute().name)
const emit = defineEmits(['response'])
emit('response', 'privacy')
</script> -->